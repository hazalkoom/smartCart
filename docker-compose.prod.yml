version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: smartcart-backend-prod
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env.prod
    restart: always
